#include<stdio.h>
#include<conio.h>
int fact(int);
void main()
{
    int num;
    printf("Enter a number : ");
    scanf("%d",&num);
    printf(" The factorial of %d is %d ",num,fact(num));
}
int fact(int num)
{
    if(num==1)
        return;
    else
        return num*fact(num-1);
}
