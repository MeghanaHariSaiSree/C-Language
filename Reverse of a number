#include<stdio.h>
int main()
{
    int temp=0,rev,num,orgnum;
    printf("Enter a number :");
    scanf("%d",&num);
    orgnum=num;
    while(num>0)
    {
        rev=num%10;
        num=num/10;
        temp=temp*10+rev;
    }
    printf("Reverse of %d is : %d ",orgnum,temp);
    return 0;
}
