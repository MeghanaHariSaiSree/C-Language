#include<stdio.h>
int main()
{
    int num,i,mul;
    printf("Enter the number : ");
    scanf("%d",&num);
    printf("Multiplication Table of %d is : \n",num);
    for(i=1;i<=10;i++)
    {
        mul=num*i;
        printf("%d X %d = %d \n",num,i,mul);
    }
    return 0;
}
